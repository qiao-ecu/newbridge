/**
 * @file encodeMsgType
 * @author cxl(chenxinle@baidu.com)
 */

/**
 * 编码消息类型
 *
 * @public
 * @param {number} type 消息类型
 * @param {number} cmd 命令类型
 * @return {number}
 */
module.exports = function (type, cmd) {
    return type << 12 | cmd;
};
